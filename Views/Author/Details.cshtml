@model MyBookApp.Models.Author

@{
    ViewData["Title"] = "Författare";
}

<h1>Detaljer</h1>

<div>
    <h4>Författare</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@Model?.Id"><i class="bi bi-pencil-square"></i>
        Redigera</a>
    <a asp-action="Index" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Tillbaka</a>
</div>
